{
  "name": "yunying-boss-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "goose:build": "node scripts/build-goose-runtime.mjs",
    "test:theme": "node scripts/theme-audit.mjs",
    "preview": "vite preview --host 127.0.0.1",
    "desktop": "tauri",
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "audit:e2e": "playwright install chromium && node scripts/final-audit.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.1",
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.9.3",
    "vite": "^8.1.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "lucide": "^1.24.0"
  }
}
