{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Core permissions required by the embedded desktop renderer.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
