# OpenMontage MCP runtime

`server.py` is the active Python MCP entrypoint. It supports Content-Length and JSONL stdio framing, the existing
low-level OpenMontage tools, short-drama tools, `resources/list`, and `resources/read` for the MCP App at
`ui://openmontage/short-drama`.

